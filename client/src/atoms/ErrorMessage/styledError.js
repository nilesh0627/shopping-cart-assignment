import styled from "styled-components";

export const StyledError = styled.div`
  color: var(--red);
  display: flex;
  align-items: center;
  margin-top: 1rem;
  > * {
    margin-right: 0.8rem;
  }
`;
